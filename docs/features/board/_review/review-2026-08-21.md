# Review — board — 2026-08-21

**Scope:** `git diff main..chore/design-canon-claude-md` — 152 files, +16084/−28 (повна фіча: api module board, web board pages/features, папір).
**Reviewers:** два незалежні clean-context агенти (stage 1 — AC-трасування; stage 2 — quality/security). Обидва звіти зведені нижче до коренів; повні тексти — у транскриптах сесії.

**Gate result: `CHANGES REQUESTED`**

## Корені (зведення 34 findings двох звітів)

| # | Корінь | Severity | Findings | Вердикт |
|---|--------|----------|----------|---------|
| A | Board змонтований на root router повз `server.New` групу: обходить rate limiter (доведено 81×200), metrics middleware (0 серій), silent-fail type assertion у `main.go:97`; handlers хардкодять абсолютні шляхи (T11 deviation) | blocker | s1: metrics, rate-limit, T11; s2: #2 #9 #18 | **Fix now**: mount-relative handlers + RouteRegistrar через `server.New`, SSE-роути окремою групою |
| B | SSE-подія без рядка `event:` — клієнт слухає іменовану `board.state_changed`, live push не працює ніколи; тести обох боків зелені ізольовано | blocker | s1 b2; s2 #1 | **Fix now** + контракт events.md + тест, що з'єднує формат сервера з підпискою клієнта |
| C | SSE рветься на 30-й секунді: `middleware.Timeout(30s)` (server.go:96) **і** `WriteTimeout: 30s` (main.go:105); нема heartbeat; нема re-fetch на reconnect (`onopen`/`onerror`) | blocker | s2 #3 #21 #26 | **Fix now**: обидві точки + keepalive + refetch on open/error |
| D | Публічний лінк будує `/public/{token}`, маршрут — `b/:token` → NotFoundPage. AC-07/09 мертві | blocker | s1 b1; s2 #4 | **Fix now** (`/b/${token}`) + тест лінк↔routes |
| E | `Column.tsx:30` заморожує `column.tasks` у `useState` — дошка не перемальовується (optimistic move, rollback, public refetch) | blocker | s2 #5; s1 major | **Fix now**: рендер із пропа |
| F | `PublicLinkPanel` не читає `BoardState.public_link` — після F5 лінк недосяжний, «Отримати» → 409 без виходу. AC-08 | blocker | s1 major; s2 #6 | **Fix now**: проп із `board.public_link` |
| G | PATCH/move віддають неповний Task (нульові `column_id`/`created_at`; move — без title/timestamps); фейк репо маскує; move не оновлює `updated_at` | major | s1 major; s2 #10 #11 #16 #25 | **Fix now**: RETURNING + повний Task (або 204 для move); чесний фейк; асерти wire-тестів |
| H | Контракт розходиться з кодом: Error envelope (вкладений vs плоский), `TaskUpdate` без required (код вимагає title), `Idempotency-Key` required але не читається, validation 400-коди поза контрактом | major | s1 minors; s2 #12 #13 #14 #28 | **Fix now** (правки openapi.yaml: envelope, `required:[title]`, прибрати Idempotency-Key, задокументувати 400-коди) |
| I | UI без error/loading: обидві сторінки → білий екран (`return null`, без `.catch`); мутації ковтають помилки; `Toaster` не змонтований на board-сторінках; title>200 → 500 замість 422 | major | s2 #15 #19 #20; s1 minor | **Fix now** |
| J | Тестові діри: жоден тест не йде через реальний `server.New`; e2e board відсутні і в CI нема e2e-джоби; navigation smoke застарілий; AC-10 без виконуваного тесту; `public_handler_test.go` без `_integration_` суфікса; move-тест не асертить «task unchanged» | major | s1: e2e, AC-10; s2 #17 #23 #33 #34 | **Fix now**: smoke через server.New, AC-10 інтеграційний тест, rename, асерти, оновити navigation smoke. **Defer**: повний e2e-through-UI набір + CI-джоба |
| K | Security/misc: rate-limit ключ по RemoteAddr (за Caddy = один бакет на всіх), токен у логах (`middleware.Logger` пише RequestURI), нема `X-Robots-Tag`, readOnly-картка з `role="button"`/drag-атрибутами, шляхи без `encodeURIComponent`, `Authorization` шлється на public-роути, 500 без slog, мертвий `ColumnExists` | major/minor | s1 major; s2 #8 #22 #24 #27 #29 #30 #31 #32 | **Fix now** (усе дешеве) |
| L | Spec §6.1 «вандалізм структурно неможливий» — неправда: мутуючі API відкриті без секрету (accepted risk ADR-0001) | major | s2 #7 | **Fix now (текст)**: чесний §6.1 + запис ризику в §8; секрет на редакторських роутах — **Defer** |

## Не-дефекти
- Хаб-буфер 1 з non-blocking send — коректний at-most-once + GET-відновлення (знято самим рев'ювером).
- Міграції: staged ↔ промоутнуті побайтово ідентичні, up/down парні, idempotent — чисто.

## Deferred (→ spec §8 Open questions)
1. Повний e2e-through-UI набір (AC-07/08/09/11) + e2e-джоба в CI — owner: genkovich, due: наступна фіча.
2. Спільний секрет на редакторських роутах поверх ADR-0001 — owner: genkovich, до першого не-воркшопного використання.
3. Кастомні board-метрики з sad §7 (SSE connections, broadcast counter) — після A базові http-метрики повертаються; лічильники — окремим PR.
4. `flush_interval -1` для SSE у Caddyfile (deploy/) — внести разом із деплой-хвилею цієї гілки.

## Стан тестів на момент рев'ю
`go build`/`go vet` чисті; unit зелені; web 142 тести зелені; інтеграційні локально не підтверджені (testcontainers env-дефект у рев'ювера); golangci-lint у рев'ювера відсутній. Жоден blocker наявними тестами не ловиться — обов'язкові пінінг-тести у фіксах.
