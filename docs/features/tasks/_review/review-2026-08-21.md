# Review record — tasks

- **Date:** 2026-08-21
- **Reviewer:** independent `sdd:reviewer` agent (clean context, read-only)
- **Author under review:** `implement tasks` (T1–T16, commits `597ea54`..`7d3cf5a`)
- **Scope:** full feature diff, `main..HEAD` at review time — 34 files across `api/internal/modules/tasks/`, `api/internal/server/`, `web/src/{entities,features,pages}/`, `docs/features/tasks/contracts/openapi.yaml`, plus the new `web/e2e/` specs.

## Verdict

**PASS.** All 13 findings resolved — 5 stage-1 (AC-blocking) and 8 stage-2 (quality/convention) findings, every one fixed via TDD (test added or strengthened first, then the fix, then re-verified green). No finding was deferred: each had a clear, bounded, correct fix rather than requiring a genuine human trade-off judgment, so the default "fix blocker/major, defer the rest" policy resolved to "fix all" in practice.

## Stage 1 — AC / correctness findings (blocking)

| # | Finding | AC / contract | Resolution |
|---|---|---|---|
| 1 | `PATCH /cards/{id}` returned a malformed response — the repo's `Update` SQL only had `RETURNING updated_at`, so `column_status`/`created_at` came back zero-valued | AC-03 (edit card) | **Fixed.** `infra/card_repository.go` `Update` now does `RETURNING column_status, created_at, updated_at` and scans all three. Strengthened `card_handler_integration_test.go` to assert the real values. |
| 2 | `fakeCardRepo.Create` (unit test double) never set `CreatedAt`/`UpdatedAt`, papering over finding #1 at the unit-test level | test adequacy | **Fixed.** `fakeCardRepo.Create` now sets both via `time.Now()`; `TestCardService_UpdateCard_NoVersionCheck` strengthened to check them. |
| 3 | Public board 404s used a JSON error envelope in the OpenAPI doc, diverging from the actual `http.NotFound` plain-text implementation | AC-05 (byte-identical 404, no board-existence leak) | **Fixed.** `contracts/openapi.yaml` now documents `text/plain` `"404 page not found"` for both public-board 404 responses. |
| 4 | Client (`BoardView.handleDrop`) treated a 404-on-move (card deleted by someone else mid-drag) as a generic error — reverted the optimistic move and showed an error toast, inverting the intended delete-wins outcome | AC-15 (concurrent delete-wins) | **Fixed.** `handleDrop` now detects `ApiClientError.statusCode === 404` and silently removes the card instead of reverting+toasting. New test: `BoardView.test.tsx` "silently drops the card on a 404 move". |
| 5 | SSE handlers (`sse_handler.go`, `public_board_handler.go`) called `Subscribe()` *after* writing response headers and flushing — a race where an event published in that window is lost by the new subscriber | AC-09/AC-10 (live board sync) | **Fixed.** Both handlers now `Subscribe()` before `setSSEHeaders`/`WriteHeader`/`Flush()`. |

## Stage 2 — quality / convention / edge-case findings

| # | Finding | Resolution |
|---|---|---|
| 6 | Generating a new public link disabled the prior one in the DB but never revoked its live SSE stream — a viewer on the old link kept receiving board updates after "revoke" | `PublicLinkRepository.Generate` signature changed to also return the replaced link's id; `PublicLinkService.GenerateLink` publishes `EventLinkDisabled` for it. New test: `TestPublicLinkService_GenerateLink_RevokesReplacedLink`. |
| 7 | Dead code: `mapPublicError`/`errCommonNotFound` in `ports/errors.go` were unused (the real 404 path uses `http.NotFound` directly) | Deleted both. |
| 8 | Rate-limit structure: the initial second-tier (high-traffic) group either panicked chi (`Route()` mounted twice) or, nested inside the standard-limit group, didn't actually raise the effective limit (middleware composes, not overrides) | Restructured to one `Route("/api/v1", …)` with two **sibling** `Group()`s — 60/min standard, 300/min high-traffic. New test: `high_traffic_test.go` (`TestHighTrafficRoutes_IndependentFromStandardLimit` exhausts the standard limit for one IP, confirms the high-traffic route still serves it). |
| 9 | `public_board_handler.go`'s `handleGet` had no cache-control header, risking a stale/cached board leaking through an intermediary after a link is disabled | Added `Cache-Control: no-store`. |
| 10 | `PublicBoardPage` rendered its own not-found UI instead of reusing the app's existing `NotFoundPage` route, diverging from `screens.md` SCR-03 | Rewrote to `navigate("/not-found", { replace: true })`, matching the manifest. |
| 11 | Drag-and-drop used HTML5 `draggable`/`dragstart`/`dragover`/`drop`, which mobile Safari/Chrome don't fire for touch gestures — the board was mouse-only despite the spec's touch NFR | Rewrote `BoardCard`/`BoardColumn`/`BoardView` on the Pointer Events API (`pointerdown`/`pointermove`/`pointerup` + `setPointerCapture`), resolved via `document.elementFromPoint` on release. Fixed an incidental bug found along the way: capturing the pointer at the card level hijacked the nested Delete button's click — skip capture when the target is inside a `<button>`. Proven end-to-end with a new Playwright `mobile` project spec, `web/e2e/tasks-board-touch.mobile.spec.ts` (real touch-typed `pointerdown`/`move`/`up` on a Pixel 7 viewport). |
| 12 | The 3-column board (~800px fixed width) overflowed a phone viewport (~412px), making the right-hand columns unreachable regardless of the touch-event fix | `flex-col sm:flex-row` container + `w-full sm:min-w-64 sm:flex-1` columns, applied to both `BoardView.tsx` and `PublicBoardView.tsx`. |
| 13 | `domain_test.go` used raw `!=`/`==` comparisons instead of `testify` assertions, inconsistent with the rest of the suite and less informative on failure | Rewritten with `require.ErrorIs`/`require.NoError`/`require.Equal`. |

## Gate result

- `npm run typecheck` (web) — clean.
- `npx vitest run` (web) — 139 tests / 21 files, all green (includes the new drag/tap/delete regression tests).
- `go build ./...`, `go vet ./...`, `go vet -tags integration ./...`, `gofmt -l .` — clean.
- `go test ./...` — all green.
- `go test -tags integration -p 1` / `-p 2` — all green (a single `-p 4` run flaked once in `internal/modules/tasks/ports`, reproduced 100% green at `-p 1`/`-p 2` — concluded Docker/testcontainers resource contention from running too many parallel Postgres containers, not a regression).
- Playwright `--project=smoke` (12 specs incl. the full `tasks-board.smoke.spec.ts` two-context lifecycle) — all green.
- Playwright `--project=mobile` (`tasks-board-touch.mobile.spec.ts`) — green, proving real touch drag-and-drop end-to-end.

**PASS** — ready for `/sdd:ship tasks`.
